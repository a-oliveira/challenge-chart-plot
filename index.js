import ReactDOM from 'react-dom';
import React    from 'react';
import Boxes from './layout.js';

/* It defines the Boxes component must be rendered on root.*/

ReactDOM.render(
    <Boxes />,
    document.getElementById('root')
);